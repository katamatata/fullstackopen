import { ADD_GOOD, ADD_NEUTRAL, ADD_BAD, RESET_STATS } from './actionTypes';

export const addGood = {
  type: ADD_GOOD,
};

export const addNeutral = {
  type: ADD_NEUTRAL,
};

export const addBad = {
  type: ADD_BAD,
};

export const resetStats = {
  type: RESET_STATS,
};
